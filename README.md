# Ponos - скриптовый язык программирования общего назначения

**Ponos** — это скриптовый язык программирования общего назначения с русскоязычным синтаксисом. Реализация написана на Rust и включает парсер, генератор байткода и виртуальную машину.

## 🎯 Особенности

- ✅ **Русскоязычный синтаксис** - все ключевые слова на русском языке
- ✅ **Winnow парсер-комбинаторы** - современная библиотека для парсинга
- ✅ **Полноценный AST** - абстрактное синтаксическое дерево со span tracking
- ✅ **Стековая ВМ** - байткод компилятор и виртуальная машина
- ✅ **Модульная система** - импорт/экспорт модулей и разрешение имён
- ✅ **71 unit-тест** - контроль за основными подсистемами

## 🚀 Быстрый старт

### Установка и запуск

```bash
# Клонировать репозиторий
git clone <url>
cd ponos-rs

# Собрать проект
cargo build

# Запустить тесты
cargo test

# Запустить REPL
cargo run

# Запустить файл
cargo run test.pns
```

### Пример кода

```ponos
// Переменные
пер x = 42;
пер имя: строка = "Иван";

// Функции
функ приветствие(имя: строка)
    возврат "Привет, " + имя + "!";
конец

// Классы
класс Точка
    x: число
    y: число

    конструктор(x: число, y: число)
        это.x = x;
        это.y = y;
    конец

    функ расстояние()
        возврат (это.x * это.x + это.y * это.y) ** 0.5;
    конец
конец

// Использование
пер точка = Точка(3, 4);
печать(точка.расстояние());  // 5.0
```

## 🏗️ Архитектура

```
Исходный код (.pns)
    ↓
┌─────────────────┐
│   Лексер        │ → Токены
└─────────────────┘
    ↓
┌─────────────────┐
│   Парсер        │ → AST (winnow комбинаторы)
└─────────────────┘
    ↓
┌─────────────────┐
│   Генератор     │ → Байткод
└─────────────────┘
    ↓
┌─────────────────┐
│   ВМ            │ → Выполнение
└─────────────────┘
```

## 🎓 Реализованные возможности

### Операторы

- ✅ Объявление переменных (`пер x = 42;`)
- ✅ Объявление функций (`функ foo(x) ... конец`)
- ✅ Классы с наследованием и интерфейсами
- ✅ Интерфейсы
- ✅ Аннотации
- ✅ Модули и импорты
- ✅ Управление потоком (if, while, return)
- ✅ Присваивание

### Выражения

- ✅ Литералы (числа, строки, булевы)
- ✅ Арифметические операторы (+, -, *, /)
- ✅ Операторы сравнения (==, !=, <, <=, >, >=)
- ✅ Логические операторы (и, или, !)
- ✅ Вызовы функций
- ✅ Доступ к полям
- ✅ Лямбды/замыкания
- ✅ `это` (this) и `родитель` (super)

### Дополнительно

- ✅ Комментарии (однострочные //, многострочные /* */)
- ✅ Поддержка Unicode (кириллица в идентификаторах)
- ✅ Span tracking для всех узлов AST
- ✅ Красивые сообщения об ошибках

## 🔧 Команды разработчика

```bash
# Форматирование кода
cargo fmt

# Линтер
cargo clippy

# Проверка без сборки
cargo check

# Запуск конкретного теста
cargo test test_parse_function

# Запуск с подробным выводом
cargo test -- --nocapture

# Release сборка
cargo build --release
```

## 📖 Полезные ссылки

- **Winnow документация:** https://docs.rs/winnow/
- **Pratt Parsing:** https://matklad.github.io/2020/04/13/simple-but-powerful-pratt-parsing.html
- **Crafting Interpreters:** https://craftinginterpreters.com/

## 📝 Лицензия

Проект распространяется под лицензией MIT (см. файл `LICENSE`).
