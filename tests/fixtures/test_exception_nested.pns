вывести("=== Тест вложенных try-catch ===");
попытка
    вывести("Внешний try");

    попытка
        вывести("Внутренний try");
        исключение "Внутренняя ошибка";
    перехват внутр
        вывести("Внутренний catch: " + внутр);
    конец

    вывести("После внутреннего try-catch");
    исключение "Внешняя ошибка";
перехват внешн
    вывести("Внешний catch: " + внешн);
конец
вывести("=== Тест завершён ===");
