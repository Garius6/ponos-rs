использовать "стд/ввод_вывод" как ио;
использовать "стд/система" как сис;

ио.вывести("=== Тест переменных окружения ===");

// Тест 1: Получение несуществующей переменной (должна вернуть ничто)
ио.вывести("Тест 1: Несуществующая переменная");
пер несуществует = сис.получить_переменную_среды("PONOS_NONEXISTENT_VAR_12345");
ио.вывести(несуществует);

// Тест 2: Установка и получение переменной
ио.вывести("Тест 2: Установка и получение");
сис.установить_переменную_среды("PONOS_TEST_VAR", "тестовое_значение");
пер тест = сис.получить_переменную_среды("PONOS_TEST_VAR");
ио.вывести(тест);

// Тест 3: Установка числовой переменной (должна преобразоваться в строку)
ио.вывести("Тест 3: Числовая переменная");
сис.установить_переменную_среды("PONOS_NUMBER", 42);
пер число_как_строка = сис.получить_переменную_среды("PONOS_NUMBER");
ио.вывести(число_как_строка);

// Тест 4: Установка булевой переменной
ио.вывести("Тест 4: Булева переменная");
сис.установить_переменную_среды("PONOS_BOOL", истина);
пер буль_как_строка = сис.получить_переменную_среды("PONOS_BOOL");
ио.вывести(буль_как_строка);

// Тест 5: Перезапись существующей переменной
ио.вывести("Тест 5: Перезапись");
сис.установить_переменную_среды("PONOS_OVERWRITE", "первое");
пер первое = сис.получить_переменную_среды("PONOS_OVERWRITE");
ио.вывести(первое);

сис.установить_переменную_среды("PONOS_OVERWRITE", "второе");
пер второе = сис.получить_переменную_среды("PONOS_OVERWRITE");
ио.вывести(второе);

// Тест 6: Получение PATH (для демонстрации)
ио.вывести("Тест 6: Получение PATH");
пер путь = сис.получить_переменную_среды("PATH");
ио.вывести("PATH получен (не выводим полностью для краткости)");

ио.вывести("=== Все тесты завершены ===");
